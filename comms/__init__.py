"""hive_nervous_system.comms — gateway, sessions, broker, registry.

Moved out of hive_mind / hive_mind_skippy per the nervous-system migration
(spark_to_bloom/src/backlog/nervous-system-migration.md). Skippy's
canonical versions of these files are the source of truth (UUID-aware
mind_id handling, rotation_check Python rewrite, SQLite BEGIN IMMEDIATE).
"""

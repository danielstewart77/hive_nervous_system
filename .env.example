# Bearer token for /memory/* and /graph/* endpoints. Empty = bypass mode
# (a startup warning is logged and all routes accept any/no Authorization
# header). Set this in production. 43+ chars recommended.
LUCENT_BEARER_TOKEN=
